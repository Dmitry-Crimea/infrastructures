---- name: make a NGINX work dir  file:    path: "{{ item.dir }}"    state: "{{ item.dir_state }}"    owner: "{{ item.owner }}"    group: "{{ item.group }}"    mode: "{{ item.mode }}"  loop: "{{ nginx }}"  tags: ['nginx']